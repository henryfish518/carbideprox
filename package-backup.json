{
  "name": "pages.top",
  "scripts": {
    "_build": "npm run _hugo-dev --",
    "_check:links": "echo IMPLEMENTATION PENDING for check-links; echo",
    "_hugo": "hugo --cleanDestinationDir",
    "_hugo-dev": "npm run _hugo -- -e dev -DFE",
    "_serve": "npm run _hugo-dev -- --minify serve --renderToMemory",
    "build:preview": "npm run _hugo-dev -- --minify --baseURL \"${DEPLOY_PRIME_URL:-/}\"",
    "build:production": "npm run _hugo -- --minify --baseURL \"https://51qpm.cn/\"",
    "check:links:all": "HTMLTEST_ARGS= npm run _check:links",
    "check:links": "npm run _check:links",
    "clean": "rm -Rf public/* resources",
    "make:public": "git init -b main public",
    "precheck:links:all": "npm run _build",
    "precheck:links": "npm run build",
    "postbuild:preview": "npm run _check:links",
    "postbuild:production": "npm run _check:links",
    "test": "npm run check:links",
    "update:dep": "npm install --save-dev autoprefixer@latest postcss-cli@latest",
    "update:pkgs": "npx npm-check-updates -u",
    "dev": "hugo server --minify"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss-cli": "^11.0.1",
    "prettier": "^3.6.2",
    "rtlcss": "^4.3.0"
  },
  "optionalDependencies": {
    "npm-check-updates": "^18.1.1"
  },
  "cSpell:ignore": "- docsy htmltest pkgs postbuild precheck refcache rtlcss -",
  "engines": {
    "node": ">=22"
  },
  "private": true,
  "prettier": {
    "proseWrap": "always",
    "semi": true,
    "singleQuote": true
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.1.0",
    "bootstrap": "^5.3.8"
  }
}
